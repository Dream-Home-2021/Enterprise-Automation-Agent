1. Feature engineering: handled categorical variables, scaled numerical features, created interaction terms.
2. Model training: split data, trained multiple models (e.g., Random Forest, XGBoost, Neural Network) with cross-validation.
3. Model evaluation: compared models using accuracy, precision, recall, F1-score, ROC-AUC; selected best model.
4. Generated graphical reports: feature importance plots, confusion matrices, ROC curves, learning curves, and saved as PDF/PNG.

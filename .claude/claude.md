请必须遵守的规则

## 文件读取

- 不要搜索整个项目。
- 不要使用 grep、glob 或 project search，除非我明确要求。
- 只读取完成当前任务所需的最少文件。
- 如果需要新的文件，请先告诉我文件名和原因，再等待确认。
- 优先根据已有上下文回答。
- 除非当前文件无法解决问题，否则不要读取 import 的文件。
- 不要递归分析整个调用链。
- 不要为了了解架构而遍历项目。

## Context

- 不要重复读取已经读取过的文件。
- 假设之前读取的内容仍然有效。
- 不要重新分析整个项目。

## 工作方式

- 每次只解决一个问题。
- 不要主动理解整个仓库。
- 不要浏览 README、docs、package.json，除非任务确实需要。
- 优先利用我提供的代码，而不是自行搜索。

## 修改

- 只修改我指定的文件。
- 如果修改会影响其他文件，先说明原因，不要直接读取。